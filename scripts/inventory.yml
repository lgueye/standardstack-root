---

- hosts: "localhost"
  connection: "local"
  roles:
  - "inventory"

- hosts: "!localhost"
  remote_user: "root"
  roles:
  - { role: "security"}

- hosts: "db:&{{ target_env }}"
  remote_user: "{{ node_management_user }}"
  sudo: "yes"
  roles:
  - "app-db"
